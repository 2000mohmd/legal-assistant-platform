__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
.ruff_cache/
dist/
build/

# Supabase CLI local secrets (e.g. RESEND_API_KEY, referenced from
# supabase/config.toml via env()). Never commit real credentials here —
# supabase/.env.example documents the expected keys instead.
supabase/.env
